{
  "name": "charts",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --files '*'",
    "build": "uglifyjs --compress --mangle --toplevel --output app.min.js app.js && echo 'gzipped size (in bytes):' && gzip -c app.min.js | wc -c"
  },
  "keywords": [],
  "author": "Yaroslav <nilfalse@yandex.com>",
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "uglify-js": "^3.5.3"
  }
}
